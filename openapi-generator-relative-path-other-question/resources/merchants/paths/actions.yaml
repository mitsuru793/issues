get:
  summary: 'List'
  operationId: listMerchant
  tags:
    - merchant
  responses:
    200:
      description: success response
      content:
        application/json:
          schema:
            type: object
            properties:
              marchants:
                $ref: '../components/schemas/Merchants.yaml'